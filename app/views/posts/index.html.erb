<h3>わたしのつぶやき</h3>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to "編集する", edit_post_path(post.id) %></td>
  </tr>
  <br>
<% end %>
<br>
<%= link_to 'つぶやく', new_post_path %>

